// Design pattren (how to solve common problems)
// singleton:-(whenever u initialize the class it returs only one object.give single object,when u ask for data it gives single object(i.e similar for) to all )
// Factory method:-creates objects without specifying the exact class to create
// Dependdencies Injections(DI):you lay the groundwork for good app architecture. 
// repositroy pattren:collect multiple instances using api and make them centralized(returning through single class)(use in web and android)
// Design Architecture: (solve the problems during codeing)(e.g MVVM,MVC,NVP)
// ORM(Object relation model/method)

// Todo App:
// -id,description,status
// -Behaviours/Features: create task,update,delete